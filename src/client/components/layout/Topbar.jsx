function Topbar() {
  return (
    <header className="client-topbar">
      <input
        type="text"
        placeholder="Search projects, documents, updates..."
      />
    </header>
  );
}

export default Topbar;